using UnityEngine;

public class TaskManager : MonoBehaviour
{
    enum Task
    {
        taskType,

    }

    //クリアしたタスクの数
    public static int clearedTaskCount = 0;
    int nowTask = 1;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
